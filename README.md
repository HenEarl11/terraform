# terraform

Infrastructure as code for the ACME web app.

Every pull request is reviewed automatically by an AI reviewer
(local Ollama model + Terraform security rules) via `.github/workflows/ai-review.yml`.
Findings are posted as inline comments with one-click **Apply suggestion** fixes where possible.
Suggestions only — nothing is changed without a human accepting it.
